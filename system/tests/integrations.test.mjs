import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp,rm} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {pathToFileURL} from 'node:url';import {Readable} from 'node:stream';import {createHmac,generateKeyPairSync,sign} from 'node:crypto';import {createIntegrations,metaSignature,discordSignature} from '../integrations.mjs';
test('HMAC rechaza mensajes modificados y firmas ausentes',()=>{const raw=Buffer.from('{}'),sig='sha256='+createHmac('sha256','secret').update(raw).digest('hex');assert(metaSignature(raw,sig,'secret'));assert(!metaSignature(Buffer.from('{ }'),sig,'secret'));assert(!metaSignature(raw,undefined,'secret'));});
test('Discord exige firma válida y timestamp reciente',()=>{const {privateKey,publicKey}=generateKeyPairSync('ed25519'),key=publicKey.export({format:'der',type:'spki'}).subarray(-32).toString('hex');const raw=Buffer.from('{}'),time=String(Math.floor(Date.now()/1000)),sig=sign(null,Buffer.concat([Buffer.from(time),raw]),privateKey).toString('hex');assert(discordSignature(raw,sig,time,key));assert(!discordSignature(raw,sig,'1',key));assert(!discordSignature(Buffer.from('bad'),sig,time,key));});
test('Meta: aislamiento de cuenta, deduplicación, persistencia y envío sin duplicados',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-channels-'));const directory=pathToFileURL(dir+'/');let sent=0;const env={META_APP_SECRET:'secret',META_VERIFY_TOKEN:'verify',META_API_VERSION:'v99.0',WHATSAPP_PHONE_NUMBER_ID:'phone',WHATSAPP_ACCESS_TOKEN:'secret-token'};const fetcher=async(url,options)=>{sent++;assert.match(url,/graph.facebook.com/);assert.equal(JSON.parse(options.body).to,'customer');return {ok:true,json:async()=>({messages:[{id:'sent-1'}]})}};try{let service=await createIntegrations({directory,env,fetcher});async function deliver(phone='phone',signature=true){const body=Buffer.from(JSON.stringify({object:'whatsapp_business_account',entry:[{changes:[{value:{metadata:{phone_number_id:phone},messages:[{id:'msg1',from:'customer',timestamp:String(Math.floor(Date.now()/1000)),type:'text',text:{body:'Hola'}}]}}]}]}));const req=Readable.from([body]);req.method='POST';req.headers={'x-hub-signature-256':signature?'sha256='+createHmac('sha256','secret').update(body).digest('hex'):''};let code;await service.webhook(req,{writeHead(c){code=c},end(){}},new URL('https://example.com/webhooks/meta'));return code;}assert.equal(await deliver('other'),200);assert.equal(service.inbox().length,0);assert.equal(await deliver('phone',false),401);await deliver();await deliver();assert.equal(service.inbox().length,1);service=await createIntegrations({directory,env,fetcher});assert.equal(service.inbox().length,1);const payload={messageId:'whatsapp:msg1',text:'Hola, te atendemos en ION Group',requestId:'request-test-00001'};await service.reply(payload);await assert.rejects(service.reply(payload),/ya fue registrado/);assert.equal(sent,1);assert.equal(service.inbox()[1].status,'accepted');assert.equal(service.status().instagram.configured,false);}finally{await rm(dir,{recursive:true,force:true});}});
test('Un fallo de red deja envío incierto y bloquea reintento con el mismo ID',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-unknown-'));try{const directory=pathToFileURL(dir+'/');const {writeFile}=await import('node:fs/promises');await writeFile(new URL('inbox.json',directory),JSON.stringify([{id:'in1',channel:'instagram',sender:'client',direction:'in',timestamp:Date.now()}]));const service=await createIntegrations({directory,env:{META_APP_SECRET:'s',META_VERIFY_TOKEN:'v',META_API_VERSION:'v99.0',INSTAGRAM_ACCESS_TOKEN:'t',INSTAGRAM_ACCOUNT_ID:'a'},fetcher:async()=>{throw Error('timeout')}});const input={messageId:'in1',text:'Hola',requestId:'request-timeout-01'};await assert.rejects(service.reply(input),/No se pudo confirmar/);assert.equal(service.inbox()[1].status,'unknown');await assert.rejects(service.reply(input),/ya fue registrado/);}finally{await rm(dir,{recursive:true,force:true});}});
test('Discord restringe servidor y usuarios antes de guardar instrucciones',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-discord-'));try{const {privateKey,publicKey}=generateKeyPairSync('ed25519');const service=await createIntegrations({directory:pathToFileURL(dir+'/'),env:{DISCORD_PUBLIC_KEY:publicKey.export({format:'der',type:'spki'}).subarray(-32).toString('hex'),DISCORD_GUILD_ID:'guild',DISCORD_ALLOWED_USER_IDS:'owner'}});async function deliver(user){const body=Buffer.from(JSON.stringify({id:'interaction1',type:2,guild_id:'guild',member:{user:{id:user}},data:{name:'ion',options:[{name:'mensaje',value:'Preparar campaña'}]}}));const time=String(Math.floor(Date.now()/1000));const req=Readable.from([body]);req.method='POST';req.headers={'x-signature-timestamp':time,'x-signature-ed25519':sign(null,Buffer.concat([Buffer.from(time),body]),privateKey).toString('hex')};let result;await service.webhook(req,{writeHead(){},end(value){result=JSON.parse(value)}},new URL('https://example.com/webhooks/discord'));return result;}
assert.match((await deliver('stranger')).data.content,/no está autorizado/);assert.equal(service.inbox().length,0);assert.match((await deliver('owner')).data.content,/Recibido/);await deliver('owner');assert.equal(service.inbox().length,1);}finally{await rm(dir,{recursive:true,force:true});}});
test('Instagram ignora otra cuenta y ecos; recibe texto de la cuenta autorizada',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-instagram-'));try{const service=await createIntegrations({directory:pathToFileURL(dir+'/'),env:{META_APP_SECRET:'s',INSTAGRAM_ACCOUNT_ID:'ion'}});for(const [account,echo,id]of [['other',false,'m1'],['ion',true,'m2'],['ion',false,'m3']]){const body=Buffer.from(JSON.stringify({object:'instagram',entry:[{id:account,messaging:[{sender:{id:'customer'},recipient:{id:account},timestamp:Date.now(),message:{mid:id,text:'Hola',is_echo:echo}}]}]}));const req=Readable.from([body]);req.method='POST';req.headers={'x-hub-signature-256':'sha256='+createHmac('sha256','s').update(body).digest('hex')};await service.webhook(req,{writeHead(){},end(){}},new URL('https://example.com/webhooks/meta'));}assert.equal(service.inbox().length,1);assert.equal(service.inbox()[0].id,'instagram:m3');}finally{await rm(dir,{recursive:true,force:true});}});
test('Facebook usa la página autorizada y responde mediante Messenger',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-facebook-'));try{let sent;const service=await createIntegrations({directory:pathToFileURL(dir+'/'),env:{META_APP_SECRET:'s',META_VERIFY_TOKEN:'v',META_API_VERSION:'v99.0',FACEBOOK_PAGE_ID:'ionpage',FACEBOOK_PAGE_TOKEN:'t'},fetcher:async(url,options)=>{sent={url,body:JSON.parse(options.body)};return {ok:true,json:async()=>({message_id:'sent'})}}});for(const page of ['other','ionpage']){const raw=Buffer.from(JSON.stringify({object:'page',entry:[{id:page,messaging:[{sender:{id:'customer'},recipient:{id:page},timestamp:Date.now(),message:{mid:'fb1',text:'Consulta'}}]}]}));const req=Readable.from([raw]);req.method='POST';req.headers={'x-hub-signature-256':'sha256='+createHmac('sha256','s').update(raw).digest('hex')};await service.webhook(req,{writeHead(){},end(){}},new URL('https://example.com/webhooks/meta'));}assert.equal(service.inbox().length,1);await service.reply({messageId:'facebook:fb1',text:'Hola',requestId:'facebook-test-0001'});assert.match(sent.url,/graph.facebook.com\/v99.0\/ionpage\/messages/);assert.equal(sent.body.messaging_type,'RESPONSE');assert.equal(sent.body.recipient.id,'customer');}finally{await rm(dir,{recursive:true,force:true});}});
