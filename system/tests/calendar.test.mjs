import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp,rm} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {pathToFileURL} from 'node:url';import {createCalendar,chileTime,eventICS} from '../calendar.mjs';
test('Fechas de Chile: desfase estacional y fechas inexistentes',()=>{assert.equal(chileTime('2026-09-22T10:00'),'2026-09-22T13:00:00.000Z');assert.equal(chileTime('2026-07-22T10:00'),'2026-07-22T14:00:00.000Z');assert.throws(()=>chileTime('2026-02-30T10:00'));assert.throws(()=>chileTime('2026-09-06T00:30'));});
test('Guardar, editar, completar y exportar sin perder persistencia',async()=>{const dir=await mkdtemp(join(tmpdir(),'ion-calendar-'));try{const url=pathToFileURL(dir+'/');let calendar=await createCalendar(url);const input={title:'Entrega, sistema',client:'Cliente',notes:'Prueba\nSegunda línea',kind:'Entrega de sistema',local:'2026-09-22T10:00',reminder:60};const event=await calendar.save(input);await calendar.save({...input,id:event.id,title:'Entrega final'});await calendar.toggle({id:event.id,done:true});calendar=await createCalendar(url);assert.equal(calendar.list().length,1);assert.equal(calendar.list()[0].title,'Entrega final');assert.equal(calendar.list()[0].done,true);const ics=eventICS(event);assert.match(ics,/DTSTART:20260922T130000Z/);assert.match(ics,/TRIGGER:-PT60M/);assert.match(ics,/SUMMARY:Entrega\\, sistema/);await assert.rejects(calendar.save({...input,reminder:-1}));}finally{await rm(dir,{recursive:true,force:true});}});
